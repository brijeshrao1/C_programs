/*Program to find the numer of prime no.s in array*/
#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
int main()
 {
 	int arr[100],i;
 	printf("Enter the size of the array\n");
 	scanf("%d",&size);
 	printf("Enter the number in the array\n");
 	for(i=0;i<size;i++)
	 scanf("%d",arr[i]);
	 
 }
